# GitHub Packages registry for @budget-buddy scoped packages
# Set GITHUB_TOKEN (read:packages scope) before installing
@budget-buddy:registry=https://npm.pkg.github.com
//npm.pkg.github.com/:_authToken=${GITHUB_TOKEN}
